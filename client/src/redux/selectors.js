// user
export const userSelector = (state) => state.user.value;

// boar
export const boardSelector = (state) => state.board.value;

// favorites
export const favoritesSelector = (state) => state.favorites.value;
